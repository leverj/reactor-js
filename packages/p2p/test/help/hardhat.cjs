const {ethers: {deployContract, getContractFactory, getSigners, provider, getContractAt}} = require('hardhat')

module.exports = {
  deployContract,
  getContractFactory,
  getSigners,
  provider,
  getContractAt
}
